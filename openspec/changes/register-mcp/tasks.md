# Tasks: Add `zolam mcp` command

- [ ] Add `newMcpCmd()` function in `src/cmd/zolam/main.go` with provider argument
- [ ] Register the command in `rootCmd.AddCommand()`
- [ ] Update README Claude Code Integration section to reference `zolam mcp claude`
- [ ] Verify with review agents
