services:
  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - ./chromadb-data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE

  rclone:
    image: rclone/rclone:latest
    volumes:
      - ./rclone-config:/config/rclone
      - ./gdrive-data:/data
    environment:
      - RCLONE_CONFIG=/config/rclone/rclone.conf
      - RCLONE_CONFIG_GDRIVE_TYPE=${RCLONE_CONFIG_GDRIVE_TYPE:-}
      - RCLONE_CONFIG_GDRIVE_CLIENT_ID=${RCLONE_CONFIG_GDRIVE_CLIENT_ID:-}
      - RCLONE_CONFIG_GDRIVE_CLIENT_SECRET=${RCLONE_CONFIG_GDRIVE_CLIENT_SECRET:-}
      - RCLONE_CONFIG_GDRIVE_TOKEN=${RCLONE_CONFIG_GDRIVE_TOKEN:-}
      - RCLONE_CONFIG_GDRIVE_TEAM_DRIVE=${RCLONE_CONFIG_GDRIVE_TEAM_DRIVE:-}
    command: sync gdrive:/ /data --progress
    profiles:
      - rclone

  ingest:
    image: ghcr.io/yetanotherchris/ingester:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./chromadb-data:/data
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - OPENROUTER_MODEL=${OPENROUTER_MODEL:-openai/text-embedding-3-small}
      - USE_LOCAL_EMBEDDINGS=${USE_LOCAL_EMBEDDINGS:-}
    depends_on:
      - chromadb
    profiles:
      - ingest
